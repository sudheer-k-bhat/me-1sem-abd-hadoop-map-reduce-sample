apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'application'

sourceCompatibility = 1.8

version = '1.0'
jar {
    manifest {
        attributes 'Implementation-Title': 'Sample MapReduce', 'Implementation-Version': version
    }
 
    baseName = 'sample-mapreduce'
    version = '0.1.0'
   from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}

repositories {
    mavenCentral()
}


configurations {
	provided
}

sourceSets {
    main.compileClasspath += configurations.provided
    test.compileClasspath += configurations.provided
    test.runtimeClasspath += configurations.provided
}

dependencies {
    provided("org.apache.hadoop:hadoop-client:3.2.1")
    provided("org.apache.hadoop:hadoop-common:3.2.1")
}


uploadArchives {
    repositories {
       flatDir {
           dirs 'repos'
       }
    }
}
